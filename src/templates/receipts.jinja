{% extends "base.jinja" %}

{% block stylesheets %}
  <link rel="stylesheet" href="{{ url_for('static', filename='assets.css') }}">
{% endblock %}

{% macro panel_content(rows=[]) %}
  {# This could be done with tables, but I don't want to play with them. #}
  <div class="panel-content">
    <div class="panel-item-col panel-button-col">
      <p></p>
      <form action="/receipt/modify" method="POST">
        {% for item in rows %}
          <div class="panel-button-wrapper">
            <button name="modify" value="{{ item['id'] }}">&#9999;</button>
            <button name="delete" value="{{ item['id'] }}">&#x267B;</button>
          </div>
        {% endfor %}
        <input type="hidden" name="csrf_token" value="{{ session.csrf_token }}">
      </form>
    </div>
    <div class="panel-item-col">
      <p>Name</p>
      {% for item in rows %}
        <p>{{ item["name"] }}</p>
      {% endfor %}
    </div>
    <div class="panel-item-col">
      <p>Date</p>
      {% for item in rows %}
        <p>{{ item["date"] }}</p>
      {% endfor %}
    </div>
    <div class="panel-item-col">
      <p>Value</p>
      {% for item in rows %}
        <p>{{ item["value"] or "0.00" }}</p>
      {% endfor %}
    </div>
    <div class="panel-item-col">
      <p>Description</p>
      {% for item in rows %}
        <p>{{ item["description"] }}</p>
      {% endfor %}
    </div>
  </div>
{% endmacro %}

{% block content%}
  <div class="content">
    <div class="panel">
      <div class="container panel-content creation-panel">
        <label class="panel-checkbox selection">
          <p class="active">Cancel</p>
          <p class="inactive">Add new receipt</p>
          <input type="checkbox">
        </label>
        <form action="/receipt/add" method="POST">
          <h1 class="container-header">Create a receipt</h1>
          <label>*<span>required</span></label>
          <input type="text"   name="receiptname" placeholder="Name"          required="true">
          <input type="text"   name="description" placeholder="Description">
          <input type="date"   name="date"        placeholder="Date">
          <input type="submit" class="selection" value="Submit">
          <input type="hidden" name="csrf_token" value="{{ session.csrf_token }}">
        </form>

        {% for message in get_flashed_messages() %}
          <p class="inform">{{ message }}</p>
        {% endfor %}
      </div>

      <hr class="content-divider">
      {{ panel_content(data) }}
    </div>
  </div>
{% endblock %}
